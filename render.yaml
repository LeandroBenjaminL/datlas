services:
  - type: web
    name: datlas-api
    runtime: docker
    region: oregon
    plan: free
    branch: main
    dockerfilePath: backend/Dockerfile
    envVars:
      - key: DEBUG
        value: "false"
