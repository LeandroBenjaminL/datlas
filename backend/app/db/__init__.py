"""
Database package — SQLAlchemy models, CRUD operations, and connection setup.

Public API:
    from app.db.database import Base, SessionLocal, engine, get_db, init_db
    from app.db.models import Dataset, AnalysisResult
    from app.db import crud
"""
